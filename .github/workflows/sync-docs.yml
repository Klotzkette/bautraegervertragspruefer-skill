name: Sync skill files to docs/

on:
  push:
    branches: [main]
    paths:
      - 'skill/SKILL.md'
      - 'skill/MINI_SKILL.md'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Copy skill files to docs/
        run: |
          mkdir -p docs
          cp skill/SKILL.md docs/SKILL.md
          cp skill/MINI_SKILL.md docs/MINI_SKILL.md

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet docs/SKILL.md docs/MINI_SKILL.md; then
            echo "docs skill files unchanged — nothing to commit."
            exit 0
          fi
          git add docs/SKILL.md docs/MINI_SKILL.md
          git commit -m "chore(docs): sync skill files for GitHub Pages"
          git push
